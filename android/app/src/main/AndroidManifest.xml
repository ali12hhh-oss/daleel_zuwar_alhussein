<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- أذونات مطلوبة لقسم "طريق زائر الحسين" (تحديد الموقع عبر GPS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- مطلوب لفتح روابط المواقع الرسمية للمراجع ولفتح خرائط جوجل -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="دليل زوار الحسين"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- يظهر خلال إقلاع فلاتر قبل رسم أول إطار -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- يخبر فلاتر أن هذا التطبيق يستخدم نظام التضمين الجديد (v2) -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- مطلوب على أندرويد 11+ لفتح تطبيق خرائط جوجل عبر url_launcher -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>
</manifest>
